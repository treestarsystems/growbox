# Test:
# cd seed/config/ansible
# ansible-playbook -i ./inventory/inventory-default.yml ./site.yml --tags seed,setup
---
- name: Setup seed device(s)
  hosts: seed
  become: true

  roles:
   - role: setup_seed
     tags:
      - setup
      - seed
